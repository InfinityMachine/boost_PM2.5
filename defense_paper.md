# 基于线性趋势与树模型残差修正的 PM2.5 预测研究

## 摘要

PM2.5 浓度预测是大气环境治理、健康风险评估和区域污染协同控制中的关键问题。针对结构化环境数据中同时存在的全局趋势、空间异质性、高基数地区类别以及局部极端污染样本等特点，本文围绕同一份 PM2.5 数据集，逐步构建并比较了多条建模路径，包括单模型 XGBoost、单模型 CatBoost、线性趋势加残差修正的两阶段模型、双残差模型加权集成、区域层级先验特征、县级行政单元消融实验以及分 regime 的 CatBoost 专家模型。研究结果表明，在当前 no-tune 设置下，`CatBoost + no-log-features + 关闭区域先验` 的路径取得了最优的全局 RMSE；与此同时，区域先验和专家模型虽然尚未在当前参数下取得最好分数，但从建模思想上为后续进一步提升模型的空间泛化能力和极端污染场景拟合能力提供了有效方向。本文的工作强调了“问题结构驱动的建模”思路，即优先围绕数据分布、空间层级和误差结构设计模型，而不是仅依赖单一算法的堆叠。

**关键词**：PM2.5 预测、CatBoost、XGBoost、两阶段建模、残差学习、区域先验、专家模型


## 1. 研究背景与意义

PM2.5 是影响空气质量和居民健康的重要污染指标。高浓度 PM2.5 与呼吸系统疾病、心血管疾病以及过早死亡风险增加密切相关。因此，构建准确、稳定、可解释的 PM2.5 预测模型，具有明显的现实意义。

与普通回归任务相比，PM2.5 预测通常有以下难点：

1. 污染形成机制复杂，受气象因子、排放前体物和区域背景共同作用。
2. 地区之间存在明显空间异质性，同样的气象条件在不同地区未必对应相同污染水平。
3. 数据中往往同时包含连续变量和高基数类别变量，例如省、市、县等行政层级。
4. 极端污染样本占比不高，但对环境治理和风险预警价值更大。

基于这些特点，本文没有只选择某一个“现成模型”直接建模，而是围绕以下问题展开：

- 如何显式分离整体趋势与局部非线性误差？
- 如何更好地利用地区层级结构？
- 如何判断某些看似细粒度的地区变量究竟是有效信息还是噪声？
- 如何在保证全局 RMSE 的同时，为高污染场景和特殊 regime 留出改进空间？


## 2. 数据集分析

### 2.1 数据集概况

当前使用的数据文件为 `data.csv`，其基本情况如下：

| 指标 | 数值 |
|---|---:|
| 样本数 | 2676 |
| 总列数 | 13 |
| 缺失值总数 | 0 |
| 目标列 | PM2.5 |

字段构成如下：

- 行政区划字段：`PROVINCE`、`CITY`、`COUNTY`
- 数值特征：`AET`、`ppt`、`tem`、`wind`、`NOX`、`SO2`、`fertilzier`、`manure`
- 目标变量：`PM2.5`
- 疑似标识字段：`OBJECTID_1`

### 2.2 目标变量统计特征

`PM2.5` 的描述性统计如下：

| 指标 | 数值 |
|---|---:|
| 均值 | 32.5177 |
| 标准差 | 13.5965 |
| 最小值 | 0.0000 |
| 25% 分位数 | 23.9250 |
| 50% 分位数 | 29.7390 |
| 75% 分位数 | 41.1000 |
| 最大值 | 93.5333 |
| 偏度 | 0.5223 |

可以看出，目标值呈现中等程度的右偏，说明高污染样本虽然存在，但目标分布整体并非极端长尾。

### 2.3 特征结构分析

数值特征的偏度显示：

- `NOX` 偏度约为 `9.07`
- `SO2` 偏度约为 `7.77`
- `fertilzier` 偏度约为 `2.59`
- `manure` 偏度约为 `2.48`

这说明部分污染前体物和农业相关变量存在明显长尾分布，可能导致：

1. 线性模型直接建模时受极端值影响较大。
2. 对数变换可能对部分模型有帮助，但不一定对所有模型都有效。

在地区层级方面：

- `PROVINCE` 唯一值数：31
- `CITY` 唯一值数：338
- `COUNTY` 唯一值数：2676

其中 `COUNTY` 的唯一值数量等于样本数，这意味着它非常接近“每条样本一个值”，因此既可能提供细粒度空间信息，也可能带来过强的记忆性和噪声。这一发现直接促成了后续的 `COUNTY` 消融实验。


## 3. 研究思路与方法探索动机

本文的建模过程不是一次性确定，而是沿着“从简单到复杂、从稳健到增强”的路径逐步推进。

### 3.1 单模型基线

首先构建两个单模型基线：

1. `XGBoost`
2. `CatBoost`

动机是建立统一的基准，回答“如果不做结构分解，仅依靠树模型本身，能达到什么性能”。

### 3.2 两阶段模型：线性趋势 + 树模型残差

由于 PM2.5 的形成既有较平滑的整体关系，也存在复杂的局部非线性和地区差异，因此引入两阶段模型：

1. 第一阶段用线性模型拟合全局趋势；
2. 第二阶段用树模型拟合残差。

对应公式为：

\[
\hat{y} = \hat{y}_{trend} + \hat{y}_{residual}
\]

这样做的动机在于：

- 让线性模型先解释稳定、可解释、全局性的关系；
- 让树模型专注于学习线性模型没有解释掉的部分。

### 3.3 OOF 趋势预测

在两阶段模型中，如果直接用“在完整训练集上拟合的趋势模型”对训练集做预测，那么每个样本都被趋势模型见过，会造成残差过于乐观，进而引入信息泄漏。

因此本文采用 OOF（Out-Of-Fold）趋势预测：

- 对每个训练样本，其趋势预测来自一个“没见过该样本”的趋势模型；
- 再用真实值减去该 OOF 趋势预测得到更真实的残差。

这一设计使残差学习更接近真实泛化场景。

### 3.4 随机验证与分组验证

由于 PM2.5 预测存在明显空间相关性，本文同时支持：

- `random`：随机切分，适用于同分布条件下的整体 RMSE 优化
- `group`：按地区分组切分，适用于评估跨城市或跨区域泛化能力

这一步的动机是避免单一验证方式带来的过度乐观评估。

### 3.5 特征工程扩展

围绕当前数据结构，本文进一步探索了以下增强方向：

1. `log1p` 特征扩展
2. 区域层级先验特征
3. `COUNTY` 消融实验
4. XGBoost 特征视图拆分
5. CatBoost regime experts

这些方法的共同出发点是：

不是简单堆模型，而是围绕数据的空间层级、分布偏态和误差结构做针对性建模。


## 4. 模型设计

### 4.1 单模型 XGBoost

单模型 XGBoost 路线将数值和类别变量统一送入预处理流水线，类别变量进行 one-hot 编码，再训练回归模型。它的优点是结构成熟、调参空间丰富，但对于高基数类别字段，表达效率未必最优。

### 4.2 单模型 CatBoost

CatBoost 路线直接利用模型对类别变量的原生支持，避免高基数类别 one-hot 后带来的维度膨胀问题。这一路线对 `PROVINCE`、`CITY`、`COUNTY` 等字段更友好，也是后续实验中表现最稳定的单模型路线。

### 4.3 线性趋势 + XGBoost/CatBoost 残差

两阶段模型的流程如下：

1. 对训练集生成 OOF 趋势预测；
2. 构造残差：

\[
r = y - \hat{y}_{trend}^{OOF}
\]

3. 训练残差模型预测 \( r \)；
4. 测试阶段：

\[
\hat{y}_{final} = \hat{y}_{trend} + \hat{r}
\]

### 4.4 双残差模型集成

对于残差模型，本文进一步尝试将 XGBoost 与 CatBoost 加权融合。为防止简单平均带来的不稳定，采用 OOF 预测搜索集成权重，并支持三种目标：

- `overall`
- `high_pm25`
- `blended`

然而从已有 no-tune 结果看，这一路线在当前配置下经常退化为“纯 CatBoost”，说明异构集成尚未形成稳定互补。

### 4.5 区域层级先验特征

区域先验的核心思想是：

- 对每个地区层级统计平滑后的目标均值和残差均值；
- 将这些统计量作为额外数值特征输入模型。

其典型形式为：

\[
prior = \frac{group\_sum + global\_mean \times smoothing}{group\_count + smoothing}
\]

本文默认使用 `PROVINCE -> CITY` 层级，并构造如下类别的特征：

1. 地区支持度特征
2. 目标先验特征
3. 残差先验特征
4. 城市相对省份的层级差值特征

这类方法的目标是帮助模型显式学习空间背景差异。

### 4.6 `COUNTY` 消融实验

由于 `COUNTY` 唯一值数过高，本文专门引入消融实验比较：

- 保留 `COUNTY`
- 去掉 `COUNTY`

这一设计的目的是验证：

- `COUNTY` 是真实有效的细粒度空间信息；
- 还是一种接近样本身份标识的高噪声特征。

### 4.7 分 regime 的 CatBoost 专家模型

为进一步提升模型对特殊样本的拟合能力，本文实现了 `catboost_experts` 路线。

设计思路如下：

1. 一个 CatBoost 专家在全部样本上训练，负责普通分布；
2. 另一个 CatBoost 专家仅在特殊 regime 上训练；
3. 在特殊 regime 内，对两个专家的输出进行加权融合。

目前支持的特殊 regime 包括：

- `trend_high`
- `low_wind`
- `trend_high_or_low_wind`

这一方法的目标不是替换单模型 CatBoost，而是提升模型对高污染或低风速等特殊场景的适应能力。


## 5. 实验设计

### 5.1 评价指标

本文主要使用以下指标：

- RMSE
- MAE
- \(R^2\)

其中，RMSE 作为主要优化目标，因为其对大误差样本更敏感，更适合 PM2.5 预测这一类实际风险导向任务。

此外，本文还会单独在高污染子集上计算：

- 高污染 RMSE
- 高污染 MAE
- 高污染 \(R^2\)

### 5.2 实验设置

在当前阶段，论文中引用的结果主要来自以下类型的实验：

1. `no-tune` 快速对比实验
2. 轻量两折或五折交叉验证实验
3. 统一切分下的多路径比较实验

在当前工作中，`random` 验证模式主要用于优化全局 RMSE；`group` 模式则更多用于泛化能力评估和稳健性分析。


## 6. 实验结果与分析

### 6.1 log 特征对比

在 no-tune 对比中，`no-log-features` 整体优于 `log-features`。代表结果如下：

| 路径 | no-log RMSE | log RMSE |
|---|---:|---:|
| `catboost` | 9.6773 | 9.8813 |
| `xgb_complementary` | 9.7896 | 9.8963 |
| `xgb_all` | 9.9612 | 10.1545 |

这说明在当前数据和当前模型设定下：

1. 树模型已经可以较好处理原始数值尺度；
2. `log1p` 扩展并未带来额外收益；
3. 反而可能因为增加冗余特征而削弱 no-tune 条件下的表现。

### 6.2 多路径 no-tune 比较

在 no-log 条件下，主要路径的 no-tune 结果如下：

| 路径 | RMSE | MAE | R2 |
|---|---:|---:|---:|
| `catboost` | 9.6773 | 7.1648 | 0.4975 |
| `ensemble_overall_all` | 9.6773 | 7.1648 | 0.4975 |
| `ensemble_overall_complementary` | 9.6773 | 7.1648 | 0.4975 |
| `xgb_complementary` | 9.7896 | 7.1659 | 0.4858 |
| `xgb_all` | 9.9612 | 7.2248 | 0.4676 |

这一结果表明：

1. 当前最优 no-tune 路径为 `catboost`
2. `ensemble` 路线在这一设置下退化为“纯 CatBoost”
3. 在 XGBoost 路线内部，`complementary` 明显优于 `all`

### 6.3 区域先验实验

在 no-tune 条件下，区域先验实验结果如下：

| 区域先验设置 | RMSE |
|---|---:|
| `off` | 9.6773 |
| `s20` | 10.3864 |
| `s15` | 10.4637 |
| `s5` | 10.4806 |
| `s10` | 10.4958 |

可以看到，当前 no-tune 条件下，开启区域先验后性能反而下降。

对此可以给出以下解释：

1. CatBoost 已经能较好利用原始地区类别；
2. 区域先验在当前参数下可能与类别信息重复；
3. 平滑后的地区均值对 no-tune CatBoost 来说可能过强，导致模型过度依赖地区统计。

因此，区域先验目前应视为“有潜力但尚未调顺”的结构化增强手段，而不是已经被证明有效的最终方案。

### 6.4 `COUNTY` 消融实验

在 `region prior = off` 的 no-tune 对比中：

| 设置 | RMSE |
|---|---:|
| `keep COUNTY` | 9.6773 |
| `drop COUNTY` | 9.7231 |

这表明在当前这一轮实验设定中，保留 `COUNTY` 略优。

但这一结论不能简单绝对化，原因在于：

- 早期快速检查中 `drop COUNTY` 曾经略优；
- 说明 `COUNTY` 的价值对实验配置较敏感；
- 它应继续作为待调特征，而不是一开始就被永久删除。

### 6.5 CatBoost 专家模型初步结果

在一次轻量 no-tune 检查中：

| 路径 | RMSE |
|---|---:|
| `catboost` | 9.7858 |
| `catboost_experts` | 9.8648 |

当前专家模型尚未超过普通 CatBoost，但这并不意味着思路无效。相反，这说明：

1. 专家模型已经具备完整训练与评估流程；
2. 当前默认 regime 和融合权重仍需进一步调优；
3. 专家模型更适合作为后续面向特殊污染场景的增强方向，而不是直接替代当前最优主线。


## 7. 研究结论

结合目前已有实验，可以得出以下结论：

1. 在当前 no-tune 条件下，`CatBoost + no-log-features + 关闭区域先验` 是最优的全局 RMSE 路径。
2. `XGBoost complementary` 优于 `XGBoost all`，说明弱化高基数地区字段、强化数值交互的思路对 XGBoost 是有效的。
3. `log1p` 特征扩展在当前树模型路线下没有带来性能提升。
4. 区域先验具有明确的方法意义，但尚未在当前参数下转化为实际收益。
5. `COUNTY` 的作用并不稳定，需要继续通过更严格的多随机种子和调参实验验证。
6. CatBoost regime experts 已经实现，是后续提升特殊场景表现的重要方向。


## 8. 创新点与工作贡献

本文的主要工作贡献可以概括为以下几点：

1. 构建了从单模型到两阶段残差模型再到专家模型的完整 PM2.5 预测实验体系。
2. 在两阶段模型中引入 OOF 趋势预测，降低了残差学习阶段的信息泄漏风险。
3. 提出了区域层级先验特征，将空间背景统计显式编码进残差模型。
4. 针对 `COUNTY` 这一超高基数行政区划字段，设计了可复用的消融实验流程。
5. 实现了分 regime 的 CatBoost 专家模型，为特殊污染场景建模提供了新方向。
6. 搭建了统一实验脚本，使不同路径、不同特征设置和不同空间先验设置可在同一划分下公平比较。


## 9. 不足与未来工作

尽管当前工作已经形成一套较完整的建模流程，但仍存在以下不足：

1. 目前许多结果仍基于 no-tune 或轻量调参，尚需更高预算搜索验证稳定性。
2. 专家模型的 regime 定义和融合权重尚未充分调优。
3. 区域先验与原始类别变量之间可能存在重复表达，尚需进一步做联合消融。
4. 当前主要基于结构化静态特征，若未来引入时间维度，可进一步拓展为时空预测模型。

未来可以重点开展以下工作：

1. 多随机种子、多折次的稳健性对比实验；
2. 围绕 `catboost_experts` 扫描 regime 和融合权重；
3. 重新评估区域先验与 `COUNTY` 是否保留的联合效果；
4. 加入不确定性估计或分位数回归，增强模型在环境决策场景中的实用性。


## 10. 总结

本文围绕 PM2.5 预测问题，构建并比较了多条从简单到复杂的建模路径。实验表明，单纯依赖“更复杂的模型”并不一定带来更好结果，真正有效的关键在于：围绕数据的空间结构、误差结构和特征分布进行有针对性的建模设计。基于当前结果，`CatBoost + no-log-features + 关闭区域先验` 是最稳健的主线；而区域先验、县级消融和专家模型则为后续进一步提升模型表现提供了扎实的实验基础和明确的研究方向。

